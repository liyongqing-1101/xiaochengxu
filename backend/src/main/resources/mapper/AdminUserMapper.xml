<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wxjiaozi.mapper.AdminUserMapper">

    <resultMap id="BaseResultMap" type="com.wxjiaozi.entity.AdminUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, nickname, role, status, created_at, updated_at
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM admin_user
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM admin_user
        WHERE username = #{username, javaType=String, jdbcType=VARCHAR}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM admin_user
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="com.wxjiaozi.entity.AdminUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin_user (username, password, nickname, role, status, created_at, updated_at)
        VALUES (#{username}, #{password}, #{nickname}, #{role}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateById" parameterType="com.wxjiaozi.entity.AdminUser">
        UPDATE admin_user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM admin_user WHERE id = #{id}
    </delete>

</mapper>
