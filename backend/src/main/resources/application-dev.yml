spring:
  datasource:
    url: jdbc:mysql://localhost:3306/exam_db?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false
  data:
    redis:
      host: localhost
logging:
  level:
    com.wxjiaozi: DEBUG
