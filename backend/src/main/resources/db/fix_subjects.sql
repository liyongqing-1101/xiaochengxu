-- ============================================================
-- 修复科目数据脚本
-- 使用场景：如果数据库中已有旧的科目数据（综合素质、教育知识与能力），
-- 需要先删除或更新为新的科目名称，才能与前端匹配
-- ============================================================

-- 方案1：删除旧科目，插入新科目（推荐）
-- 注意：先检查是否有题目关联，如果有需要先处理题目
-- DELETE FROM exam_subject WHERE category_id = 1;

-- 插入正确的4个科目（与前端FIXED_SUBJECTS一致）
INSERT IGNORE INTO exam_subject (id, category_id, name, icon, sort_order, status) VALUES
(1, 1, '高等教育学', '📖', 1, 1),
(2, 1, '高等教育法规和政策', '⚖️', 2, 1),
(3, 1, '教师伦理学', '🎓', 3, 1),
(4, 1, '大学心理学', '🧠', 4, 1);

-- 方案2：如果已经有题目数据，更新科目名称
-- UPDATE exam_subject SET name = '高等教育学', icon = '📖' WHERE id = 1;
-- UPDATE exam_subject SET name = '高等教育法规和政策', icon = '⚖️' WHERE id = 2;
-- 然后插入id=3和4的新科目
