spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/exam_db?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=true
  data:
    redis:
      host: ${REDIS_HOST:localhost}
logging:
  level:
    com.wxjiaozi: INFO
